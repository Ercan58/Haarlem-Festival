@model HF_Application.Models.ViewModel.JazzsModel

@{
    ViewBag.Title = "Jazz at Patronaat";
}

<body id="Jazz">
    <div class="container">
        <div class="col-md-8 offset-md-2 col-12 welcomediv">
            <h1 class="text-center">Jazz at Patronaat!</h1>
            @*<p class="welcometext">
                Reservation is mandatory.  A reservation fee of €10,- per person wil be charged when a reservation is made on the Haarlem Festival site. This fee will be deducted from the final check on visiting the restaurant.
            </p>*@
        </div>
        <div class="row restaurantnavigation">
            @*<div class="col-md-12 col-12 text-center">
                @foreach (var jazz in Model.DaySelectionFilter)
                {
                    @Html.ActionLink(jazz, "index", new { id = jazz }, new { @class = "DinerNavigationButton" })
                }
            </div>*@

            <div class="col-md-12 col-12 text-center">

                @Html.ActionLink("All", "Index", new { date = "" }, new { @class = "DinerNavigationButton" })
                @Html.ActionLink("26-07-2018", "Index", new { date = "2018-07-26"}, new { @class = "DinerNavigationButton" })
                @Html.ActionLink("27-07-2018", "Index", new { date = "2018-07-27" }, new { @class = "DinerNavigationButton" })
                @Html.ActionLink("28-07-2018", "Index", new { date = "2018-07-28" }, new { @class = "DinerNavigationButton" })
                @Html.ActionLink("29-07-2018", "Index", new { date = "2018-07-29" }, new { @class = "DinerNavigationButton" })
      
            </div>
        </div>
    </div>

        @*<div class="container">
            @foreach (var jazzEvent in Model.AllJazzEvents)
            {
                <div class="row jazzDiv">
                    <div class="col-md-12 text-center"><h1 class="jazzName">@jazzEvent.Band</h1></div>
                    <div class="col-md-3">
                        <img class="jazzImage" src="@Url.Content("~/Content/images/"+jazzEvent.imagePath)" alt="Image of @jazzEvent.Band" />
                    </div>
                    <div class="col-md-6 col-12 text-center">
                        <strong>Date:</strong> @jazzEvent.StartDate.ToShortDateString()<br />
                        <strong>Time:</strong> @jazzEvent.StartDate.ToShortTimeString() - @jazzEvent.EndDate.ToShortTimeString()<br />
                        <strong>Adress:</strong> @jazzEvent.Location.Address
                    </div>
                    <div class="col-md-3 col-12 text-center">
                        @using (Html.BeginForm())
                        {
                            <input type="button" name="button" class="MoreInformationButton" value="Meer informatie" />
                        }
                    </div>
                </div>
            }

        </div>*@

    <div class="container-fluid">
        <div class="row">
            @foreach (var jazzEvent in Model.AllJazzEvents)
            {
                <div class="col-lg-4 col-md-4 col-sm-6 col-xl-3">
                    <div class="jazzDiv">

                        <div class="col-md-12">
                            <img class="jazzImage" src="@Url.Content("~/Content/images/"+jazzEvent.imagePath)" alt="Image of @jazzEvent.Band" />
                        </div>

                        <div class="col-md-12 text-center"><h1 class="jazzName">@jazzEvent.Band</h1></div>

                        <div class="row">
                            <div class="col-sm-6 text-center">
                                <div>
                                    <h2>Date</h2>
                                </div>
                                @jazzEvent.StartDate.ToShortDateString()
                            </div>

                            <div class="col-sm-6 text-center">
                                <div>
                                    <h2>Time</h2>
                                </div>
                                @jazzEvent.StartDate.ToShortTimeString() - @jazzEvent.EndDate.ToShortTimeString()
                            </div>

                            <div class="col-md-12 col-12 text-center tastebuttoncol">
                                @using (Html.BeginForm())
                                {
                                    <input type="button" name="button" class="MoreInformationButton" value="More Information" />
                                }
                            </div>

                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</body>